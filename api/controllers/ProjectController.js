const {
  create,
  view,
  update,
  remove,
  fetchAll,
  addMember,
  removeMember,
} = require('../services/project');

module.exports = {
  create,
  view,
  update,
  remove,
  fetchAll,
  addMember,
  removeMember,
};
