const {
  addMember,
  remove,
  fetch,
  fetchAll,
  register,
  update,
  updatePassword,
} = require('../services/user');

module.exports = {
  addMember,
  remove,
  fetch,
  fetchAll,
  register,
  update,
  updatePassword,
};
